# Copilot Instructions

## Project Guidelines
- User prefers the coding agent to use a locally hosted model exposed on a port instead of loading the model directly from ONNX files.
- User prefers xUnit for tests and wants integration tests that always run and fail if Foundry Local is unavailable, rather than mocked or skipped connectivity tests.